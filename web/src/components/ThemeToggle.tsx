// DESIGN.md specifies a dark-only surface. The toggle used to swap
// between light/dark; we keep the export so external imports don't
// break, but it renders nothing. If we ever re-introduce a light
// theme, this is where the control goes.
export function ThemeToggle() {
  return null;
}
